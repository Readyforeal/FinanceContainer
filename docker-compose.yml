services:
  app:
    build: .
    container_name: better-with-90
    restart: unless-stopped
    ports:
      - "8080:80"
    env_file: .env
    volumes:
      - app-storage:/app/storage
    networks:
      - homelab

volumes:
  app-storage:

networks:
  homelab:
    external: true
